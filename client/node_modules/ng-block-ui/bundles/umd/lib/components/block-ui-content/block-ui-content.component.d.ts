import { OnInit, OnDestroy } from '@angular/core';
import 'rxjs/add/operator/map';
import { BlockUIService } from '../../services/block-ui.service';
export declare class BlockUIContentComponent implements OnInit, OnDestroy {
    private blockUI;
    name: string;
    defaultMessage: string;
    active: boolean;
    private message;
    private blockUISubscription;
    constructor(blockUI: BlockUIService);
    ngOnInit(): void;
    private subscribeToBlockUI(blockUI$);
    private onDispatchedEvent(event);
    private onStart(event);
    private onStop(event);
    ngOnDestroy(): void;
}
